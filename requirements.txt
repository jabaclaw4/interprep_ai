# ===== ЭТАП 1: БАЗОВЫЙ БОТ (МИНИМАЛЬНЫЕ ЗАВИСИМОСТИ ДЛЯ РАБОТЫ) =====
# Основные фреймворки
aiogram==3.8.0             # Асинхронный фреймворк для Telegram бота
aiohttp==3.9.0             # Асинхронный HTTP клиент/сервер
python-dotenv==1.0.1       # Загрузка переменных окружения из .env файла
sqlalchemy==2.0.29         # ORM для работы с базой данных

# Утилиты и парсинг
certifi==2024.2.2          # SSL сертификаты
charset-normalizer==3.3.2  # Определение кодировки
idna==3.6                  # Поддержка IDNA
multidict==6.0.4           # Мультисловари для aiohttp
yarl==1.9.4                # URL объекты для aiohttp
typing-extensions==4.9.0   # Поддержка аннотаций типов

# ===== ЭТАП 2: БАЗОВЫЙ AI И RAG ФУНКЦИОНАЛ =====
# LangChain и работа с документами
langchain==0.1.0
langchain-community==0.0.10
pypdf==3.17.4              # Чтение PDF файлов

# Дополнительные утилиты для LangChain
PyYAML==6.0.1              # YAML парсинг для конфигураций
tqdm==4.66.1               # Прогресс-бары для обработки файлов
numpy==1.26.3              # Числовые операции (часто требуется для ML библиотек)

# ===== ЭТАП 3: ВЕКТОРНАЯ БАЗА И ЭМБЕДДИНГИ =====
# ChromaDB для векторного поиска
chromadb==0.4.22

# Эмбеддинг модели (легкая локальная альтернатива OpenAI)
sentence-transformers==2.2.2

# Зависимости для ChromaDB и sentence-transformers
httpx==0.26.0              # Асинхронный HTTP клиент
posthog==3.0.1             # Аналитика (используется ChromaDB)
tokenizers==0.15.1         # Токенизация текста
huggingface-hub==0.20.3    # Доступ к моделям HuggingFace
onnxruntime==1.16.3        # Runtime для ONNX моделей (нужен для sentence-transformers)

# ===== ЭТАП 4: ОПЦИОНАЛЬНО - OPENAI И ПРОДВИНУТЫЕ МОДЕЛИ =====
# OpenAI API (раскомментировать при необходимости)
# openai==1.12.0
# tiktoken==0.6.0          # Токенизатор для GPT моделей

# ===== ОБЩИЕ УТИЛИТЫ И ИНСТРУМЕНТЫ =====
# Логирование
loguru==0.7.2              # Удобное логирование

# Сериализация
ujson==5.8.0               # Быстрый JSON парсинг

# Дата/время
pytz==2024.1               # Часовые пояса

# Тестирование (для разработки)
pytest==7.4.4
pytest-asyncio==0.21.1
pytest-cov==4.1.0